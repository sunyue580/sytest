#' Add together two numbers.
#'
#' @param x A number
#' @param y A number
#' @return The sum of \code{x} and \code{y}.
#' @examples
#' add2(1, 1)
#' add2(10, 1)
add2 <- function(x, y) {
  x + y
}
